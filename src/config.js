/** Edita esta constante para cambiar el mensaje del aro. */
export const DEDICATION = 'Te elegiría a ti. Una y mil vidas más.';

/** Ajustes opcionales de la experiencia. */
export const ROSE_CONFIG = {
  autoRotate: true,
  // Negative speed moves the visible sentence from right to left for reading.
  rotationSpeed: -0.38,
  petalColor: '#8e071f',
};
